services:
  - type: web
    name: casio-tool
    runtime: python
    plan: free
    buildCommand: pip install -r requirements.txt && node generate-images.js
    startCommand: gunicorn run:app
    envVars:
      - key: PYTHON_VERSION
        value: 3.9.0